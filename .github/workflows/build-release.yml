name: Build Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to build (e.g. 2.2.8) — leave blank to use tag'
        required: false

concurrency:
  group: build-release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  guard:
    name: Require tag or version input
    runs-on: ubuntu-latest
    steps:
      - name: Check trigger
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ -z "${{ github.event.inputs.version }}" ]; then
            echo "ERROR: workflow_dispatch requires a version input."
            echo "Use a tag push (v*.*.*) or provide a version in the dispatch form."
            exit 1
          fi

  resolve-version:
    runs-on: ubuntu-latest
    needs: guard
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - id: version
        run: |
          RAW="${{ github.event.inputs.version || github.ref_name }}"
          echo "version=${RAW#v}" >> "$GITHUB_OUTPUT"

  build-zip:
    uses: ./.github/workflows/job-build-zip.yml
    needs: resolve-version
    with:
      version: ${{ needs.resolve-version.outputs.version }}
      artifact-name: rich-statistics
      retention-days: 30

  upload-freemius:
    name: Upload ZIP to Freemius
    runs-on: ubuntu-latest
    needs: [resolve-version, build-zip]
    if: startsWith(github.ref, 'refs/tags/')

    steps:
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'
          extensions: curl, json, mbstring

      - name: Checkout (for deploy script + SDK)
        uses: actions/checkout@v4

      - name: Download ZIP artifact
        uses: actions/download-artifact@v4
        with:
          name: rich-statistics-${{ needs.resolve-version.outputs.version }}
          path: build

      - name: Determine channel
        id: channel
        run: |
          if echo "${{ github.ref_name }}" | grep -q "beta"; then
            echo "release_mode=beta" >> $GITHUB_OUTPUT
          else
            echo "release_mode=released" >> $GITHUB_OUTPUT
          fi

      - name: Copy ZIP to workspace root
        run: |
          cp "build/rich-statistics-${{ needs.resolve-version.outputs.version }}.zip" "rich-statistics-${{ needs.resolve-version.outputs.version }}.zip"

      - name: Deploy to Freemius via SDK
        env:
          DEV_ID: ${{ secrets.FREEMIUS_DEV_ID }}
          PUBLIC_KEY: ${{ secrets.FREEMIUS_PUBLIC_KEY }}
          SECRET_KEY: ${{ secrets.FREEMIUS_SECRET_KEY }}
          PLUGIN_SLUG: rich-statistics
          PLUGIN_ID: 25954
        run: |
          php bin/deploy-freemius.php \
            "rich-statistics-${{ needs.resolve-version.outputs.version }}.zip" \
            "${{ needs.resolve-version.outputs.version }}" \
            "${{ steps.channel.outputs.release_mode }}"

  release:
    name: PWA snapshot + GitHub Release
    runs-on: ubuntu-latest
    needs: [resolve-version, build-zip]
    permissions:
      contents: write
      actions: read
    if: startsWith(github.ref, 'refs/tags/')

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download ZIP artifact
        uses: actions/download-artifact@v4
        with:
          name: rich-statistics-${{ needs.resolve-version.outputs.version }}
          path: build

      - name: Read SHA256 checksum
        id: checksum
        run: |
          HASH=$(awk '{print $1}' "build/rich-statistics-${{ needs.resolve-version.outputs.version }}.zip.sha256")
          echo "sha256=${HASH}" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            build/rich-statistics-${{ needs.resolve-version.outputs.version }}.zip
            build/rich-statistics-${{ needs.resolve-version.outputs.version }}.zip.sha256
          generate_release_notes: true
          body: |
            ## Rich Statistics ${{ needs.resolve-version.outputs.version }}

            See [CHANGELOG.md](CHANGELOG.md) for details.

            ### SHA256 Checksum
            ```
            ${{ steps.checksum.outputs.sha256 }}
            ```

            ### Freemius
            Automatically uploaded as a new version via CI.

      - name: Bump SW cache name
        run: |
          # Update the service worker cache name to match the release version.
          # This forces all browsers to invalidate the old cache and fetch fresh assets.
          VERSION="${{ needs.resolve-version.outputs.version }}"
          CACHE_NAME="rsa-$(echo "$VERSION" | tr '.' '-')"
          sed -i "s/: 'rsa-[0-9]\+-[0-9]\+-[0-9]\+'/: '${CACHE_NAME}'/" docs/app/sw.js

      - name: Create versioned PWA snapshots (stable + beta)
        run: |
          VERSION="${{ needs.resolve-version.outputs.version }}"
          for CHANNEL in stable beta; do
            APP_VERSIONED="docs/app/v/${VERSION}/${CHANNEL}"
            if [ ! -d "$APP_VERSIONED" ]; then
              mkdir -p "$APP_VERSIONED"
              for f in index.html app.js app.css config.js sw.js sw-init.js manifest.json chart.min.js; do
                [ -f "docs/app/${f}" ] && cp "docs/app/${f}" "$APP_VERSIONED/${f}"
              done
              for f in index-dev.html index-test.html config-dev.js config-test.js; do
                [ -f "docs/app/${f}" ] && cp "docs/app/${f}" "$APP_VERSIONED/${f}"
              done
              [ -d "docs/app/icons" ] && cp -r "docs/app/icons" "$APP_VERSIONED/icons"
            fi
          done

      - name: Update versions.json
        run: |
          # Regenerate versions.json from the v/ directory — never manually edit.
          # Lists only stable channel versions.
          python3 << 'PYEOF'
          import json, pathlib
          v_dir = pathlib.Path('docs/app/v')
          versions = sorted(
              [d.name for d in v_dir.iterdir() if d.is_dir()],
              key=lambda x: list(map(int, x.split('.')))
          )
          pathlib.Path('docs/app/versions.json').write_text(json.dumps(versions))
          # Also generate versions-beta.json for the beta channel
          # (pointing to the same version directories — beta snapshots are subdirs)
          pathlib.Path('docs/app/versions-beta.json').write_text(json.dumps(versions))
          PYEOF

      - name: Prune old snapshot directories (keep last 12)
        run: |
          # Keep the 12 most recent versioned snapshots. Older ones are removed
          # from both the v/ directory and versions.json.
          python3 << 'PYEOF'
          import json, shutil, pathlib
          v_dir = pathlib.Path('docs/app/v')
          all_v = sorted(
              [d.name for d in v_dir.iterdir() if d.is_dir()],
              key=lambda x: list(map(int, x.split('.')))
          )
          keep = set(all_v[-12:])
          for d in v_dir.iterdir():
              if d.is_dir() and d.name not in keep:
                  shutil.rmtree(d)
                  print(f"pruned {d}")
          # Regenerate version JSONs from what remains
          remaining = sorted(
              [d.name for d in v_dir.iterdir() if d.is_dir()],
              key=lambda x: list(map(int, x.split('.')))
          )
          pathlib.Path('docs/app/versions.json').write_text(json.dumps(remaining))
          pathlib.Path('docs/app/versions-beta.json').write_text(json.dumps(remaining))
          PYEOF

      - name: Commit and push PWA snapshots to main
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/app/v/ docs/app/versions.json docs/app/versions-beta.json docs/app/sw.js
          git diff --cached --quiet && exit 0
          git commit -m "chore: add PWA snapshot v${{ needs.resolve-version.outputs.version }} and prune old versions"
          git fetch origin main
          git rebase origin/main
          git push origin HEAD:main

  build-desktop:
    uses: ./.github/workflows/job-build-desktop.yml
    needs: release
    if: startsWith(github.ref, 'refs/tags/')
    with:
      target-path: /var/www/rs-app/public_html/dist
      server-host: app.richstatistics.com
      updater-url: https://app.richstatistics.com/dist/update.json
      apt-script: rsa-apt-repo-update
      update-json-script: rsa-gen-update-json
      artifact-prefix: rich-statistics
      stamp-version: true
    secrets: inherit

  ping-deploy:
    name: Deploy PWA to production
    runs-on: ubuntu-latest
    needs: [release, build-desktop]
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Sync docs/app/ via deploy webhook
        run: |
          set -euo pipefail
          max_retries=3
          retry=0
          while [ $retry -lt $max_retries ]; do
            echo "Webhook attempt $((retry + 1))/$max_retries..."
            if curl -fsS -X POST \
              -H "X-Deploy-Token: ${{ secrets.DEPLOY_WEBHOOK_TOKEN }}" \
              https://app.richstatistics.com/_deploy/; then
              echo "Webhook succeeded."
              break
            fi
            retry=$((retry + 1))
            if [ $retry -lt $max_retries ]; then
              echo "Webhook failed, retrying in 10s..."
              sleep 10
            else
              echo "ERROR: Webhook failed after $max_retries attempts."
              exit 1
            fi
          done

      - name: Smoke test — verify production server is serving PWA
        run: |
          echo "Waiting 5s for deploy to settle..."
          sleep 5
          STATUS=$(curl -fsS -o /dev/null -w "%{http_code}" https://app.richstatistics.com/ || true)
          if [ "$STATUS" = "200" ]; then
            echo "Smoke test passed — production server responding with HTTP 200."
          else
            echo "WARNING: Production server returned HTTP ${STATUS} — deploy may be incomplete."
          fi
